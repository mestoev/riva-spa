// Moved to /lib/stats.ts (shared by web admin + this bot).
// This file is kept as a thin re-export for backwards compat — can be deleted later.
export { getStats, type Period, type Stats } from "../../lib/stats";
